# Полный аудит проекта: NEAR Sovereign AI Blueprint — App 1 Crypto Treasury
**Дата аудита:** 2026-05-07  
**Аудитор:** Claude (Cowork)

---

## 1. Общее описание проекта

**Цель:** Построить систему «крипто-казначейства» для EDU-Arbitrage модели на NEAR Protocol. Система принимает NEAR от инвесторов/грантов, конвертирует в USDC, блокирует в эскроу-контракте и отдаёт только после подтверждения от App 2 (Fiat Settlement). Школы никогда не знают, что используется криптовалюта.

**Состоит из двух приложений:**
- **App 1: Crypto Treasury** — Rust смарт-контракт + Node.js/Express backend (в разработке)
- **App 2: Fiat Settlement + School Portal** — Node.js/React (не начато)

---

## 2. Структура файлов (фактическое состояние)

```
near_projectcop/
├── docs/
│   ├── PROGRESS.md                          ← трекер прогресса (устарел, см. ниже)
│   └── superpowers/
│       ├── specs/2026-05-05-crypto-treasury-design.md   ← дизайн-документ ✅
│       └── plans/2026-05-06-app1-crypto-treasury.md     ← пошаговый план ✅
│
├── app1-crypto-treasury/
│   ├── .gitignore                            ✅ создан
│   └── contract/
│       ├── Cargo.toml                        ✅ near-sdk = "=5.26.1"
│       ├── build.log                         ❌ содержит ошибку (cargo не найден)
│       └── src/lib.rs                        ✅ частично реализован
│
├── near_project/                             ← пустое Obsidian-хранилище
├── NEAR_Sovereign_AI_Blueprint.pdf           ← исходный Blueprint
├── NotebookLM Mind Map.png                   ← майнд-карта
└── NEAR AI_ Суверенный ИИ и Блокчейн ...html ← сохранённый чат Gemini
```

**ОТСУТСТВУЕТ полностью:**
- `app1-crypto-treasury/backend/` — директория не создана (Tasks 5–10 не начаты)

---

## 3. Детальный аудит: что сделано

### Task 1 — Scaffold контракта ✅ ЗАВЕРШЕНО
- `Cargo.toml` создан с правильными зависимостями (near-sdk 5.26.1)
- `src/lib.rs` — базовая структура файла создана
- `.gitignore` создан
- Git инициализирован (3+ коммита)

### Task 2 — Data Structures + lock_funds ✅ ФАКТИЧЕСКИ ЗАВЕРШЕНО
> ⚠️ **PROGRESS.md неактуален** — он помечает Task 2 как "⏳ NEXT", но код уже содержит полную реализацию.

Реализовано в `contract/src/lib.rs`:
- `PaymentStatus` enum (Locked / Released) с `#[near(serializers = [borsh, json])]`
- `Payment` struct (payment_id, amount_usdc, status)
- `EscrowContract` с `UnorderedMap<String, Payment>` и полем `owner`
- `EscrowContract::new(owner)` — инициализация
- `lock_funds(payment_id, amount_usdc)` — с проверками дубликатов и нулевой суммы
- `get_payment(payment_id)` — поиск по ID
- `get_all_payments()` — список всех платежей
- 5 unit-тестов (все проходят логически по коду)

**Использован корректный near-sdk 5.x API** (`#[near(contract_state)]`, `#[near]`, `near_sdk::store::UnorderedMap`) — правильное решение, т.к. 4.x не работает из-за yanked зависимости.

---

## 4. Детальный аудит: что НЕ сделано

### Task 3 — release_funds ❌ НЕ РЕАЛИЗОВАНО
В `lib.rs` **полностью отсутствует** метод `release_funds`. Это критичная функция всей системы — без неё USDC нельзя разблокировать.

Нужно добавить:
```rust
pub fn release_funds(&mut self, payment_id: String) {
    let mut payment = self.payments.get(&payment_id)
        .expect("Payment not found")
        .clone();
    assert!(payment.status == PaymentStatus::Locked, "Payment is not in LOCKED status");
    payment.status = PaymentStatus::Released;
    self.payments.insert(payment_id, payment);
}
```
И 3 теста: успешный release, паника на несуществующий ID, паника на двойной release.

### Task 4 — Build WASM + Deploy ❌ НЕ ВЫПОЛНЕНО
`build.log` содержит ошибку: `cargo: command not found` — сборка не проводилась.  
Требует ручных шагов (Rust toolchain, near-cli, аккаунт в testnet).

### Tasks 5–10 — Node.js Backend ❌ НЕ НАЧАТО
Вся директория `backend/` отсутствует. Нужно создать:
- `backend/package.json`
- `backend/src/config.js`
- `backend/src/wallet.js`
- `backend/src/swap.js`
- `backend/src/escrow.js`
- `backend/src/index.js`
- `backend/tests/*.test.js` (4 файла)

### App 2: Fiat Settlement ❌ НЕ СПРОЕКТИРОВАНО
Дизайн-документ App 2 не создан. Указан только в разделе "Что дальше" спеки App 1.

---

## 5. Найденные проблемы и риски

| # | Проблема | Критичность | Где |
|---|----------|-------------|-----|
| 1 | `PROGRESS.md` не обновлён — Task 2 помечен как NEXT, а он уже DONE | Низкая | docs/PROGRESS.md |
| 2 | `build.log` содержит ошибку (cargo not found) — нужно проверить среду | Средняя | contract/build.log |
| 3 | Отсутствует `release_funds` — без него весь escrow-контракт неполон | Высокая | contract/src/lib.rs |
| 4 | Нет access control на `release_funds` — любой может разблокировать средства | Высокая | Запланировано как gap |
| 5 | `WNEAR_USDC_POOL_ID = 2` — pool ID для testnet не верифицирован | Средняя | plan Task 8 |
| 6 | USDC баланс не возвращается в `GET /api/balance` — только NEAR | Низкая | plan Known Gaps |
| 7 | Нет аутентификации на `POST /api/release` — API открыт | Высокая | plan Known Gaps |
| 8 | `.env` с реальными ключами нельзя коммитить — нужно проверить .gitignore | Средняя | backend/.gitignore |

---

## 6. Дорожная карта: что делать дальше

### Этап 1: Завершить смарт-контракт (1–2 дня)

**Step 1.** Обновить `PROGRESS.md` — Task 2 → ✅ DONE

**Step 2.** Добавить `release_funds` в `contract/src/lib.rs` с тестами (Task 3)

**Step 3.** Добавить access control на `release_funds`:
```rust
assert_eq!(env::predecessor_account_id(), self.owner, "Only owner can release");
```

**Step 4.** Установить Rust toolchain локально и собрать WASM (Task 4):
```powershell
rustup target add wasm32-unknown-unknown
cd contract
cargo build --target wasm32-unknown-unknown --release
```

**Step 5.** Создать testnet аккаунт и задеплоить контракт:
```bash
near login
near create-account escrow.yourname.testnet --masterAccount yourname.testnet --initialBalance 5
near deploy --accountId escrow.yourname.testnet --wasmFile target/wasm32-unknown-unknown/release/escrow.wasm --initFunction new --initArgs '{"owner": "yourname.testnet"}'
```

---

### Этап 2: Node.js Backend (2–3 дня) — Tasks 5–10

Создать в правильном порядке:

1. **Task 5:** `backend/package.json`, `config.js`, `.env.example` — Project Setup
2. **Task 6:** `wallet.js` — loadAccount, getNEARBalance
3. **Task 7:** `escrow.js` — lockFunds, releaseFunds, getPayment, getAllPayments
4. **Task 8:** `swap.js` — wrapNEAR, swapNEARtoUSDC (верифицировать pool_id на testnet.ref.finance)
5. **Task 9:** `index.js` — Express API (5 эндпоинтов + тесты)
6. **Task 10:** E2E тест через curl на testnet

---

### Этап 3: Безопасность перед production (до mainnet)

- [ ] Добавить API key middleware на `POST /api/release`
- [ ] Добавить `ft_balance_of` для USDC баланса в `/api/balance`
- [ ] Проверить/обновить Ref Finance pool ID для mainnet
- [ ] Rate limiting на API
- [ ] Логирование транзакций

---

### Этап 4: App 2 — Fiat Settlement (следующий этап)

- [ ] Написать дизайн-документ App 2 (аналогично `specs/2026-05-05-crypto-treasury-design.md`)
- [ ] Спроектировать React UI для школ (без упоминания крипты)
- [ ] Интеграция с Wise/Airwallex для выплат в фиате
- [ ] Webhook: при успешной выплате → вызов `POST /api/release` в App 1

---

## 7. Итоговый статус

| Компонент | Готовность | Статус |
|-----------|-----------|--------|
| Дизайн-документ App 1 | 100% | ✅ |
| Пошаговый план App 1 | 100% | ✅ |
| Rust контракт — структуры данных | 100% | ✅ |
| Rust контракт — lock_funds | 100% | ✅ |
| Rust контракт — release_funds | 0% | ❌ |
| Rust контракт — сборка WASM | 0% | ❌ |
| Rust контракт — деплой testnet | 0% | ❌ |
| Node.js Backend | 0% | ❌ |
| App 2 Fiat Settlement | 0% | ❌ |

**Общая готовность App 1:** ~25%  
**Следующий конкретный шаг:** добавить `release_funds` в `lib.rs` → Task 3

---

*Сгенерировано автоматически на основе анализа кодовой базы и документации.*
